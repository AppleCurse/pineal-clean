# Opsiyonel ML / anlamsal bellek bağımlılıkları.
#
# Ana requirements.txt'e DAHİL EDİLMEZ — bunlar yalnızca anlamsal (semantik)
# bellek aramasını (HindsightMemory) etkinleştirmek isteyenler içindir.
#
# Kurulum:
#   pip install -r requirements-ml.txt
#
# Kurulmadığında sistem CanonicalMemory ile çalışmaya devam eder; kanıt saklama
# hiçbir zaman bu paketlere bağlı değildir.

sentence-transformers>=2.2.0
