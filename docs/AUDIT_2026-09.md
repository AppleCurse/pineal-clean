# PINEAL-HERETIC v3.1 — Bağımsız Mimar & QA Adli Denetim Raporu (Eylül 2026)

**Tarih:** 18 Eylül 2026  
**Kapsam:** `backend/api.py`, `agent_core/task_executor.py`, `agent_core/services/`, `agent_core/agents/`, `frontend/`, `Dockerfile`, `tests/`  
**Referans PR:** [PR #91 (AppleCurse/pineal-clean)](https://github.com/AppleCurse/pineal-clean/pull/91) / Commit: `8fa3f464`  
**Nihai Durum:** MERGED (`ceeb645b`) · **Test Suite:** 1.183 passed / 2 skipped / 0 failed · `ruff check .` temiz

> **Defter kuralı (2026-09-21):** Bu dosya artık **kanonik denetim defteridir**; yukarıdaki 18 Eylül raporu tarihsel gövdesi, altındaki Ek'ler ise yaşayan defterdir. Kural ve Ek'ler için dosya sonundaki **"EK DEFTERİ"** bölümüne bakın.

---

## 1. Yönetici Özeti (Executive Summary)

Bu denetim raporu, canlıya alınacak `AppleCurse/pineal-clean` deposunun üretim güvenliği, mimari bütünlüğü, fail-closed sözleşmeleri ve çoklu istemci izolasyonu ekseninde gerçekleştirilen acımasız adli inceleme sonucunda hazırlanmıştır.

Yapılan incelemede tespit edilen **1 P0 (Kritik Veri Sızıntısı)**, **5 P1 (Yüksek Risk)** ve **10 P2 (Orta Risk)** madde doğrudan kod tabanında çözülmüş, 21 yeni entegrasyon testiyle kilitlenmiş ve ana dalda (`main`) kalıcılaştırılmıştır.

---

## 2. Düzeltilen ve Kanıtlanan Kritik Açıklar (P0 & P1)

### 🔴 P0-1: WebSocket Cross-Room Telemetri ve Veri Sızıntısı
* **Dosya & Konum:** `backend/api.py` (`_send_ws`)
* **Sorun:** Odanın FIFO göndericisi `_send_ws`, `all_ws.update(r["websockets"])` döngüsüyle o an sistemdeki tüm odaların soketlerini toplayıp yayını herkese gönderiyordu. İstemci A'nın hedef profil analizi, LLM telemetrisi ve kanıt zinciri, istemci B'nin tarayıcısına canlı olarak sızıyordu.
* **Çözüm:** Yayın katı şekilde `room.get("websockets", set())` kümesine sınırlandı. `_WS_SEND_TIMEOUT_S = 5.0` zaman aşımı ve asılı/yanıt vermeyen soketleri anında temizleyen evict mekanizması eklendi.
* **Kanıt Testi:** `tests/integration/test_live_fixes_2026_09.py::test_ws_room_isolation_no_leak` (`leak_probe.py` ile sızıntı 0'a indirildi).

### 🟠 P1-2: Yavaş/Ölü Tüketici Soket Kilitlenmesi (Head-of-Line Blocking)
* **Dosya & Konum:** `backend/api.py` (`_send_ws`)
* **Sorun:** `await ws.send_text(payload)` çağrısında zaman aşımı yoktu. Bir istemcinin ağı yavaşladığında veya bağlantı koptuğunda FIFO gönderici süresiz asılı kalarak ilgili odanın tüm telemetrisini donduruyordu.
* **Çözüm:** `asyncio.wait_for(..., timeout=5.0)` ile sınırlama getirildi; zaman aşımına uğrayan soket güvenle odadan düşürüldü.
* **Kanıt Testi:** `tests/integration/test_live_fixes_2026_09.py::test_ws_send_timeout_and_eviction`.

### 🟠 P1-3: Zombie Chromium ve Playwright Driver Süreç Sızıntısı
* **Dosya & Konum:** `backend/api.py` (`_close_room`) & `agent_core/services/browser_session.py`
* **Sorun:** Oda silinirken (`_evict_rooms`, 30 dk TTL), `room["browser"]` nesnesi `close()` çağrılmadan bellekten atılıyordu. Node.js Playwright sürücüsü ve Chromium süreçleri işletim sisteminde zombi olarak kalıp uzun çalışma sürelerinde OOM çökmesine yol açıyordu.
* **Çözüm:** `_close_room` içine `session.close()` çağıran detached asenkron görev eklendi; kapanışlar `_detached_tasks` havuzuna bağlandı.
* **Kanıt Testi:** `tests/integration/test_live_fixes_2026_09.py::test_close_room_cleans_browser_session`.

### 🟠 P1-5a: Sınırsız JSON Gövdeleri Üzerinden DoS Riski
* **Dosya & Konum:** `Dockerfile` (CMD)
* **Sorun:** Starlette/Uvicorn varsayılan olarak gövde boyutu sınırı uygulamaz. Doğrulanmış bir istemci dahi 100 MB+ JSON göndererek bellek tüketimine (OOM DoS) neden olabilirdi.
* **Çözüm:** Uvicorn başlatma komutuna `--limit-max-request-size 1048576` (1 MiB tavan) parametresi eklendi.

### 🟠 P1-5b: `/api/override` Sınırsız Kural Birikimi ve Prompt Şişmesi
* **Dosya & Konum:** `agent_core/services/memory_injector.py` & `backend/api.py`
* **Sorun:** `learnings.json` dosyasına eklenen operatör kuralları için tavan yoktu. Her kural her görevin her LLM prompt'una enjekte edildiği için prompt boyutları kontrolden çıkma riski taşıyordu.
* **Çözüm:** `OverridePayload` içine `fact` (≤ 2000 karakter) ve `tag` (≤ 64 karakter) sınırı kondu. `MemoryInjector` aktif kuralları maksimum **50 kural** ile sınırlandırdı; taşma durumunda dürüst kesim telemetri notu bırakıldı.
* **Kanıt Testi:** `tests/integration/test_live_fixes_2026_09.py::test_memory_injector_bounded_rules`.

### 🟠 P1-6: Scraper Hatası Yutulması ve Boş Hedefle Sahte Analiz
* **Dosya & Konum:** `backend/api.py` (`run_mission`)
* **Sorun:** Kazıma sırasındaki altyapı çökmeleri (timeout, driver hatası) sessizce yutulup görev boş bir profil ile yürütülüyordu. Sistem hedefsiz veri üzerinde çalışıp operatöre yanlış "Yetersiz Kanıt" terminal durumu bildiriyordu.
* **Çözüm:** Hata yakalaması `isinstance` ile gerçek sınıflara (`InsufficientEvidenceError`, `ScraperInsufficientEvidenceError`) bağlandı. `PINEAL_SCRAPE_MAX_ATTEMPTS` ile üstel geri çekilmeli retry döngüsü kuruldu. Altyapı tükenmesinde sahte analize girmek yerine görev dürüstçe `failed` ("HEDEF VERİSİ ALINAMADI") durumuna çekildi.
* **Kanıt Testi:** `tests/integration/test_live_fixes_2026_09.py::test_scraper_retry_and_honest_terminal_failure`.

---

## 3. Mimari ve Epistemik Onarımlar (P2 & İş Mantığı)

1. **OSINT Discovery Fazına Taşındı (Madde 1):** `task_executor.py` içerisinde satır 991'deki geç OSINT adımı kaldırılarak keşif fazına (görsel analiz sonrası, rota öncesi) taşındı. Çıktılar `input_data["public_osint"]` olarak derinlik motoruna ve ajanlara bağlandı.
2. **Sağır Odalar ve Deterministik Upstream Bulgu Köprüsü (Madde 3):** `agent_core/services/upstream_findings.py` servisi kuruldu. Ajan başına ≤280 karakter, toplam ≤2000 karakter FIFO bütçesiyle `passion_mapper`, `friction_detector`, `cognitive_profiler` ve `resonance_synthesizer` ajanlarına "doğrulanmamış çıkarım" etiketiyle besleme yapıldı.
3. **Aspasia Deterministik Hüküm Özeti (Madde 4):** `agent_core/services/verdict_synthesizer.py` servisi ile 0 LLM maliyetiyle kanıt-iddia eşleştirmesini özetleyen ≤2000 karakterlik deterministik HÜKÜM BELGESİ Aspasia'nın sistem promptuna entegre edildi.
4. **Cloudflare Reverse-Proxy & Edge Yapılandırması (Madde 5):** `wrangler.toml`, `functions/api/[[path]].ts`, `functions/ws/[[path]].ts` ve `docs/DEPLOYMENT_CLOUDFLARE.md` ile Pages mimarisi tamamlandı.
5. **Kanonik Bellek Retry Bulaşması (P2-1 / Madde 9):** `CanonicalMemory._resolve_conflicts` metodunda, retry senaryolarında eski denemeye ait kayıtların `(agent, evidence_type, source_agent)` anahtarıyla üzerine yazılması (supersede) sağlandı.
6. **query_json Repair Tuzağı (P2-3 / Madde 10):** `llm_gateway.py` içine `ProviderEmptyResponseError(RuntimeError)` eklendi. Taşıyıcı katmandaki boş yanıtlar artık JSON ayrıştırma hatası sanılarak ikinci kez ücretli tamir çağrısına sokulmuyor.
7. **FREKANS Mekanik Etiketi (Madde 11):** `task_executor.py:734` satırındaki yanıltıcı frekans ifadesi `INSUFFICIENT_RESONANCE_EVIDENCE` mekanik etiketine dönüştürüldü. 5 testin kilitlediği `"halted_frequency"` statü kodu geriye dönük uyumluluk için korundu.
8. **Ek Sertleştirmeler:**
   - `/v1` streaming yanıtlarında bağlantı kopmalarına karşı `chunks.aclose()` garanti edildi.
   - `_scraper_capability` Playwright sürücüsünü lüzumsuz başlatmaktan kurtarılıp `PINEAL_CHROMIUM_PATH` üzerinden kısa devreye alındı ve TTL 300 saniyeye çıkarıldı.
   - `ResponseCache` kapalıyken gereksiz SQLite bağlantı denemeleri engellendi.
   - `BrowserSession.open()` yönlendirmeler sonrası nihai URL alan adı izin listesine karşı yeniden doğrulandı (open-redirect koruması).

---

## 4. Çürütülen ve Geri Çekilen İddialar

* **`social_proof: 0.72` İddiası (ÇÜRÜTÜLDÜ):** Depo genelinde yapılan ripgrep/grep taramalarında `social_proof` ifadesine 0 eşleşme bulundu. Kod tabanında hiçbir zaman bu isimde bir etiket yer almamıştır.
* **P1-4 Bulgusu (GERİ ÇEKİLDİ):** "result_error ham mesajı WS'e sızıyor" iddiası incelendiğinde, WebSocket yükünün yalnızca `{type, status, task_id}` taşıdığı; ham hata metninin ise yalnızca maskeli sunucu loguna aktarıldığı doğrulanarak bulgu geri çekilmiştir.

---

## 5. Doğrulama & Test Metrikleri

- **Toplam Test Sayısı:** 1.185
- **Geçen (Passed):** 1.183
- **Atlanan (Skipped):** 2 (Canlı harici LLM anahtarı gerektiren e2e testleri)
- **Hata (Failed):** 0
- **Linter & Biçimlendirme:** `ruff check .` ➔ All checks passed.
- **Entegrasyon Paketi:** `tests/integration/test_live_fixes_2026_09.py` (21 test)

---

## 6. Canlı Dağıtım (Production GO / NO-GO) Hükmü

| Alan | Durum | Gerekçe |
|---|---|---|
| **P0 Veri İzolasyonu** | **GO** | Çoklu istemci sızıntısı giderildi, odalar hermetik olarak izole edildi. |
| **İşletimsel Kaynaklar** | **GO** | Zombi Chromium ve bellek sızıntıları bertaraf edildi, gövde sınırı kondu. |
| **Epistemik Doğruluk** | **GO** | Scraper dürüstlüğü ve deterministik kanıt sentezi sağlandı. |
| **Cloudflare Edge** | **Aksiyon Gerekli** | Cloudflare Pages panelinde `Root directory: frontend` ve `BACKEND_ORIGIN` env değişkeni girilmelidir. |

**Nihai Karar:** Sistem canlıya çıkış için **ONAYLANMIŞTIR (GO)**.

---

# EK DEFTERİ — kanonik tek defter (2026-09-21'den itibaren)

## Defter kuralı (uzlaştırma kararı)

**Bulgunun özeti:** 2026-09-21 itibarıyla iki paralel denetim defteri vardı: (1) bu dosya, `main` üzerinde, 18 Eylül raporuyla donmuş hâlde; (2) denetçi oturumunun **push edilmemiş yerel ağacında** aynı yol üzerinde 401 satırlık Ek 1–4 sürümü (Ek 3 @ `:340`). İkisi de doğruydu; dosya ağaçta yaşıyordu, defter hiçbir ağaçta ortak değildi.

**Karar:**

1. **Kanonik defter = bu dosya, bu ağaç (`main`).** Ek'ler buraya eklenir; hiçbir oturum kendi ağacında ayrı defter başlatmaz. Push erişimi olmayan oturum, defteri klonlayıp Ek'ini kendi ağacında **geçici** tutar ve bir sonraki push-erişimli oturuma graft listesi devreder.
2. **Numaralandırma ardışık ve globaldir.** Ek 1–4 slotları denetçi oturumunun (2026-09-21) defterine aittir ve burada rezerve edilmiştir; içeriği graft edilene kadar bu defter onların yerine içerik UYDURMAZ.
3. **Kanıt fiilleri iki kelimedir:** `DOĞRULANDI` (bu ağaçta, tanımlı ortamda koşularak; koşu kimliği yazılır) ve `BİLDİRİLDİ` (başka oturum/ağaçtan aktarılan sayı; koşulamadıysa öyle kalır). Yeşil ama açıklamasız sayı deftere girmez.
4. **Bir sayı deftere "ortamıyla" girer** (Python/deps/OS kısa etiketi). Skip sayıları ortam-bağımlıdır; iki oturumun toplam test farkı ortam farkından olabilir (bkz. Ek 5.2).

---

## Ek 5 — Devir teslim oturumu: çapraz okuma + uygulama (2026-09-21)

**Bu oturum:** push + CI erişimi var; GitHub çalışıyor. Önceki oturumların (röntgen/fix: `arena/01a0c190`, merge PR #96 `ae31026`; denetçi: push edilmemiş ağaç) kayıtları üzerinden çalıştı.

### Ek 5.1 — "Önce" tablosu ve kovalamaca deseni

Denetçinin Ek 4'ünden aktarılan ölçüm (düzeltilmemiş kod): **1. iddia: hiç koltuk düşmedi → 2. iddia: "open" düştü → 3. iddia: "claude" düştü.**

**Durum: mekanizma DOĞRULANDI (kod-okuma), sayısal dizi BİLDİRİLDİ.**

- Merge-öncesi `main` (`2edc9bbb`) kaynak kodunda `autonomous_verifier` içinde `PANEL_AGENTS`/`_verify_with_panel` **0 eşleşme**; iki zincir çağrısı var (iddia çıkarma `:82`, tek yargı `:144`). Yani 1. iddiada düşme olmaması **yapısal zorunluluktur** — koltuk diye bir şey yoktu.
- 2. ve 3. iddiadaki sıralı düşüş (üreten = önceki iddianın son jürisi) fix oturumunun probe koşusudur; mekanizma Ek 3'ün kod-okumasıyla tutarlı, sayısal dizi deftere fix oturumu raporu olarak BİLDİRİLDİ kalır (probe betiği ağaçta değildi).

### Ek 5.2 — "+80 test" delta'sının hesabı (devir listesi maddesi 3)

**Bulgular:**

1. `1267` ve `5 yeni test` dizgeleri **reponun hiçbir yerinde geçmez** — fix oturumunun yalnız sohbetinde kalmış sayılardı. Aşağıda hem BİLDİRİLDİ hem DOĞRULANDI sürümleri yazılıdır.
2. Anlatılan "5 yeni test" aslında **yalnız jüri düzeltmesine ilişkin bir eksik sayım**dı: PR #96, `test_verifier_jury_panel.py` içinde **7** test getirdi (BOSS-4); ayrıca B1–B12 + BOSS-13 kapanışına ait **9 yeni test dosyası + 6 güncellenen dosya** taşıdı. Aynı dal iki işi birlikte getirdiği için "önce→sonra" farkı anlatılan işten büyüktü.

**Aynı ortamda ölçülen yapısal delta (DOĞRULANDI, bu oturum):**

| Dosya (PR #96'nın test katkısı) | Önce | Sonra |
|---|---|---|
| `tests/audit/test_boss2_ghost_task_lock.py` (yeni) | — | 12 |
| `tests/unit/test_9router_config_contract.py` (yeni) | — | 9 |
| `tests/unit/test_verifier_jury_panel.py` (yeni, BOSS-4 jüri) | — | 7 |
| `tests/audit/test_boss8_agent_timeout.py` (yeni) | — | 6 |
| `tests/unit/test_ui_honesty_contract.py` (yeni) | — | 6 |
| `tests/audit/test_boss11_frame_contract.py` (yeni) | — | 5 |
| `tests/audit/test_boss7_b9_wiring.py` (yeni) | — | 5 |
| `tests/unit/test_pillar_transport_contract.py` (yeni) | — | 4 |
| `tests/unit/test_resonance_synthesizer.py` (B3 sözleşmesi) | 2 | 6 |
| `tests/unit/test_config_contract.py` | 6 | 8 |
| `tests/unit/test_gorev2_depth_engine.py` | 18 | 20 |
| `tests/audit/test_boss6_authentic_vector_telemetry.py` (yeni) | — | 2 |
| **Toplam (collected)** | | **+64** |

**Tam paket, aynı ortam, koşulu (DOĞRULANDI):** merge-öncesi `main` (`2edc9bbb`) **1183 passed / 4 skipped** → fix dalı + bu oturumun katı-kural düzeltmesi **1250 passed / 4 skipped** (`pytest -q -rs`, Python **3.11.2** sandbox, `requirements.txt` birinci adım; yaklaşık 70 sn).

> **Düzeltme (Ek 6, 2026-09-21):** bu satırda önceden "Python 3.12 sandbox" yazıyordu — **yanlış etiketti**. Sandbox `python3 --version` → **3.11.2**; üretim `Dockerfile:20` → `python:3.11-slim`; CI `ci.yml:23` → `"3.11"`. Üçü hizalıdır. Ayrıca bu çiftin ağacı `requirements.txt`'tir; **CI'ın kapısı değildir** (bkz. Ek 6.1).

**Fix oturumunun ortamı (BİLDİRİLDİ):** 1187/2 → 1267/4. Ortamlar arasında toplam ~2 (önce) ve ~17 (sonra) testlik fark vardır: opsiyonel bağımlılıklara (cv2/ml vb.) bağlı parametrizasyon ve skip koşulları. **Ders:** ham test sayısı ortamla taşınır; defter kuralı 4 gereği sayılar ortam etiketiyle yazılır.

### Ek 5.3 — Skip'lerin güncel kimliği (eski şerhin düzeltilmesi)

18 Eylül raporu "2 skipped = canlı LLM anahtarı gerektiren e2e" diyordu. Bugünkü 4 skip'in tamamı **opsiyonel bağımlılık koşullu**dur (DOĞRULANDI, `-rs` çıktısı): `crawl4ai` ikinci-adım ×2 (`test_consolidation_faz1_5.py:143`, `test_no_mock_in_production.py:138`), `PIL` None-dalı ×1 (`test_gorev2_artiklar.py:158` — "PIL kurulu; None-dalı bu ortamda çalışmaz"), `open-interpreter` ×1 (`test_no_mock_in_production.py:130`). Canlı-anahtar bekleyen skip **yok**. Eski açıklama güncelliğini yitirdi.

### Ek 5.4 — Katı zincir-ailesi düşürme kuralı (devir listesi maddesi 4, opsiyonel → uygulandı)

**Rezidü (kodla doğrulandı):** düzeltme sonrası `_producing_family` kayıttan, kayıt yoksa **zincir başından** okuyordu. Zincir `autonomous_verifier = [anthropic/claude-sonnet-5, x-ai/grok-4.6]`; bugünkü koltuk seti (google/anthropic/open_weights) için sonuç nötr, ama zincir **yedeği** (grok/xai) üretirse ve gelecekte xai koltuğu eklenirse baş-tabanlı tespit o koltuğu düşürmezdi → kendini-onaylama geri dönerdi.

**Kırmızı testli kanıt (DOĞRULANDI, `tests/unit/test_verifier_jury_panel.py`):**

1. `test_chain_backup_family_seat_is_dropped` — **önce KIRMIZI** (xai koltuğu düşmedi), sonra yeşil.
2. `test_strict_rule_is_practically_neutral_today` — **önce KIRMIZI** (rapor `üreten_aile=anthropic` diyerek aile kümesini eksik yazıyordu), sonra yeşil: rapor `üreten_aile=anthropic+xai` yazıyor ve bugünkü düşen koltuk seti değişmiyor (`[pineal_juror_claude]`).
3. `test_producer_record_path_drops_actual_family` — **baştan yeşil ama yeni kilit**: kayıt yolunu (üretim defterinin tuttuğu dünyayı) sahte gateway hiç açmıyordu; denetçinin ikinci itirafındaki kapsam açığı bu testle kapandı.

**Uygulama:** `AutonomousVerifier._producing_families` — şüpheli aile kümesi = kayıttaki fiilî üretenin ailesi ∪ zincirin **tüm** halkalarının aileleri; `unknown` hiçbir koltuğu düşüremez. `_verify_with_panel` koltuğu kümedeki herhangi bir aileyle düşürür. Yön muhafazakârdır: en kötü durumda üretmemiş bir koltuk düşer.

### Ek 5.5 — Dürüstlük şerhi (bu Ek için)

- DOĞRULANDI: Ek 5.2'nin tablosu, 1183/4 → 1250/4 çifti, skip kimlikleri, kırmızı→yeşil koşuları, ruff (`All checks passed!`), PR #96'nın merge CI'ı (run `35561413308`, success), bu Ek'i getiren PR #97'nin CI'ı (run `35582467328`: backend/frontend/android/rust-core/smoke → success).
- **CI şerhi (PR #97):** Cloudflare "Workers Builds" ×2 fail — **yapısal ve önceden var**: aynı iki kontrol PR #96'nın merge commit'inde ve bugünkü `main` head'inde de fail (0 sn: PR dalı için hiç koşmuyor, dış sistem). Bu PR'ın kırığı değildir; ama bir sonraki oturumun gündemine değer: dış sistem defterle konuşmuyor.
- BİLDİRİLDİ: fix oturumunun 1267/%85/5-test üçlüsü (sohbette kalmış; %85'in kaynağı bu defterde yoktur — kökeni fix oturumuna sorulmalı), Ek 5.1'deki 2.–3. iddia dizesi, denetçinin Ek 1–4 içeriği (graft bekliyor).

### Ek 5.6 — Altın küme: sonraki cephenin tanımı (bu oturumda kanıtı ÜRETİLEMEZ)

Açık tespit denetçininkiyle aynı: bugüne kadarki tüm "çalışıyor" kanıtı **mekanik** (çağrı oldu, şema tuttu, koltuk düştü); çıktının **iyiliği** hiç sınanmadı. Bu sandbox'ta sağlayıcı anahtarı yoktur; anahtarsız "altın küme koştum" iddiası, bu projenin doktrinine aykırı olarak yeşil yanılsaması olurdu — üretilmedi. Sonraki oturum için asgari iskelet: (1) 20–50 gerçek biyodan sabit altın küme; (2) puanlama kriteri operatörle mühürlenmiş rubrik (sahicilik, kanıta bağlılık, çelişki); (3) rubrikli değerlendirme ayrı ve **güçlü** jüri ailesiyle; (4) sonuçlar bu deftere ortam + maliyet + ham çıktı referansıyla yazılır. Anahtar gerektiren her adım `BİLDİRİLDİ`/koşulamadı olarak işaretlenir.

---

## Ek 6 — Bağımsız çapraz doğrulama: CI'ın gerçek kapısı (2026-09-21)

Bu Ek, Ek 5'i yazan oturumdan **ayrı bir oturumda** ve ayrı bir sandbox'ta koşulmuştur. Amaç: Ek 5'in sayılarını tekrar üretmek değil, **doğru ağaçta** ölçmek. Tüm rakamların arkasında bu sandbox'ta koşulmuş çıktı vardır.

**Ortam etiketi (defter kuralı 4):** Python **3.11.2** / Debian Linux / `requirements.lock` + `ruff pytest pytest-asyncio pytest-cov` / izole venv (repo dışına kurulmadı, ölçüm sonrası silindi).

### Ek 6.1 — Ağaç hatası: `requirements.txt` CI'ın kapısı değildir

Ek 5.2'nin 1183/4 → 1250/4 çifti `requirements.txt` ile ölçülmüştür. **CI ve üretim bu ağacı kurmaz:**

| Yer | Kurulan |
|---|---|
| `.github/workflows/ci.yml:30` | `pip install -r requirements.lock ruff pytest pytest-asyncio pytest-cov` |
| `.github/workflows/ci.yml:88` | `pip install -r requirements.lock` |
| `.github/workflows/release-gates.yml:39` | `pip install -r requirements.lock` |
| `Dockerfile:37` | `pip install -r requirements.lock` |

`ci.yml:26-28`'deki yorum bu farkı zaten belgelemiş: *"CI eskiden requirements.txt'i taze çözüyordu → prod ile test edilen ağaç farklıydı (ölçülen: 20 paket sürüm farkı, 2 test skip/geçer farkı)."*

**Sonuç:** `requirements.txt` ile alınan her sayı, üretimin ağacına ait değildir. Ek 5.2'nin çifti yanlış değil ama **yanlış eksende**; bundan sonra kapı sayısı lock ağacından yazılır.

### Ek 6.2 — CI backend kapısının birebir koşusu (DOĞRULANDI)

`ci.yml:34`'ün komutu değiştirilmeden koşuldu:

```
PYTHONPATH=$PWD pytest -q -p no:cacheprovider --cov=agent_core --cov=backend \
  --cov-report=term-missing:skip-covered --cov-fail-under=80
```

```
TOTAL                    12466   1837    85%
Required test coverage of 80% reached. Total coverage: 85.26%
1252 passed, 2 skipped, 2 warnings in 79.95s
```

**Kapı sayısı: 1252 / 2, coverage %85.26 ≥ 80 → PASS.**

### Ek 6.3 — "%85" gizemi kapandı

Ek 5.5'te *"%85'in kaynağı bu defterde yoktur — kökeni fix oturumuna sorulmalı"* şerhi düşülmüştü. **Kaynak bulundu:** `%85`, coverage kapısının çıktısıdır (`Total coverage: 85.26%`; komşu ölçümde 85.24%). Fix oturumunun sayısı uydurma değildi — **farklı eksenin sayısıydı** ve test sayısı sanılarak taşınmıştı.

**Ders (kural 4'ün genişletilmesi):** bir sayı yalnız ortamıyla değil, **birimiyle** de taşınır. "%85" bir test sayısı değil, bir kapsam yüzdesidir.

### Ek 6.4 — "1267" aritmetiğine şerh

Ek 5.2 farkı ortam kaynaklı (opsiyonel bağımlılıklar) saymıştı. Doğru ağaç belirlendikten sonra aritmetik kayar: txt ağacı **1250**, lock ağacı **1252**. Dolayısıyla "1250 + 17 = 1267" yerine "1252 + 15 = 1267" de aynı toplamı verir. Toplam açıklanabilir olsa da **fark rakamı ağaç seçimine bağlıdır**; sabit bir delta olarak alıntılanmamalıdır. `1267`'nin ağacı hâlâ **BİLDİRİLDİ** sınıfındadır (o ağaç bu sandbox'ta mevcut değil).

### Ek 6.5 — Lint kapısı ve bir ölçüm tuzağı (DOĞRULANDI)

`ruff check .` repo kökünde → **`All checks passed!`**

**Tuzak kaydı:** venv repo kökünün *içine* kurulursa ham `ruff check .` **7 hata** verir — hepsi `\u003cvenv\u003e/share/doc/networkx-2.8.8/examples/` altındaki üçüncü-parti örnek dosyalardan (F401, F821). Bunlar repo kodu değildir; CI temiz checkout'ta koştuğu için etkilenmez. Ölçen oturumlar venv'i repo dışına kurmalı veya `--exclude` vermelidir; aksi halde "ruff kırmızı" diye yanlış alarm üretilir.

### Ek 6.6 — Cloudflare "Workers Builds" (DOĞRULANDI: repo dışı)

`grep -rn "pineal-gland"` → repoda **hiç geçmiyor** (`.git/` hariç tüm ağaç). Fail eden iki Worker kontrolünü repo konfigürasyonu tanımlamıyor. **Repodan düzeltilemez**; temizlik Cloudflare dashboard tarafındadır. Ek 5.5'teki "yapısal ve önceden var" şerhi kod tarafından teyit edilmiştir.

### Ek 6.7 — nltk maruziyeti (DOĞRULANDI: P3-izleme)

`requirements.lock:93` → `nltk==3.10.3`. Kod taraması: `grep -rn "import nltk\|from nltk" --include=*.py` → **sıfır eşleşme**. Paket lock'ta var, çalışan kodda kullanılmıyor → maruziyet düşük, launch engeli değil, izleme maddesi.

### Ek 6.8 — Dürüstlük şerhi (bu Ek için)

- **DOĞRULANDI (bu sandbox'ta koşuldu):** Ek 6.1'in tablosu (dosyalardan okundu), Ek 6.2'nin 1252/2 + %85.26 çıktısı, Ek 6.5'in `All checks passed!` ve 7-hatalı tuzak, Ek 6.6'nın boş grep'i, Ek 6.7'nin lock satırı + sıfır import, Python 3.11.2 / 3.11-slim / "3.11" hizası.
- **DOĞRULANMADI (başka oturumda bildirildi, burada koşulmadı):** xdist paralel flake taraması, `pip-audit` bulgusu ve `PYSEC-2026-3740` kimliği, `requirements-interpreter.txt` ↔ `requirements-osint.txt` ResolutionImpossible, sır taraması, 1183/4 tarihsel ölçümü. Bunlar makuldür ancak bu Ek'in kanıt kümesinde yoktur.
- **KAYIP İŞ:** Bir önceki oturum Ek 6'yı `e512c8f8` commit'iyle yazdığını bildirmişti. O commit **ne uzakta ne yerelde bulunabildi** (`gh api` → 422; `git cat-file` → not a valid object) ve defter bu PR'dan önce **188 satırdı**. İçerik kaybolmuştur; bu Ek, kovalamak yerine bulguları **yeniden ölçerek** yazılmıştır. Aynı desen `kalan_duzeltmeler.patch` için de geçerlidir: taşıyıcısı olmayan sandbox işi, iş değildir.

### Ek 6.9 — Açık kalemler (bu oturumdan sonra)

1. Cloudflare dashboard temizliği (repo dışı, operatör işi).
2. `nltk` izleme — fix sürümü çıkarsa lock güncellenir.
3. Altın küme (Ek 5.6) — anahtar gerektirir, hâlâ üretilmemiştir.
4. Ek 1–4 graft (denetçi içeriği).

---

## Ek 7 — P0-Boot Tuzağı Kapanışı: `BodySizeLimitMiddleware` (2026-09-21)

### Ek 7.1 — P0 Bulgusu: Uvicorn CMD Parametre Uyuşmazlığı (Crash-Loop)

`Dockerfile:77` komutunda bulunan `--limit-max-request-size 1048576` parametresi, lock dosyasında sabitlenmiş olan `uvicorn==0.52.4` tarafından **tanınmamaktadır**:
```
Error: No such option '--limit-max-request-size'.
```
Bu bayrakla başlatılan herhangi bir Docker/Railway konteyneri derleme sonrası boot anında exit code 2/1 ile anında çökmekteydi (Railway 3 yeniden başlatma sonrası servisi öldürmekteydi).

### Ek 7.2 — CI Smoke Körü

CI üzerindeki `smoke` işi uvicorn'u Dockerfile'daki CMD ile değil, yalın parametrelerle (`uvicorn backend.api:app --port 8000`) ayağa kaldırdığı için bu ölümcül çöküş CI testlerinden tamamen gizli kalmıştır (Yeşil Smoke ≠ Çalışan Konteyner).

### Ek 7.3 — Onarım ve Mimari Taşıma

1. **Uygulama Katmanı Güvencesi:** Tavan sınırlaması web sunucusu bayrağından uygulama katmanına taşındı. `backend/api.py` içine `BodySizeLimitMiddleware` eklendi ve `app` üzerine kaydedildi.
   - Varsayılan limit: **1 MiB (1048576 bayt)**.
   - Dinamik override: `PINEAL_MAX_BODY_BYTES` ortam değişkeni ile ayarlanabilir.
   - Erken ret: `Content-Length > 1 MiB` ise istek gövdesi okunmadan doğrudan **HTTP 413** döner.
   - Akış (Chunked) koruması: Content-Length başlığı içermeyen akışlarda gövde okunurken kümülatif boyut 1 MiB'ı aştığı anda akış kesilerek **HTTP 413** ile dürüst hata gövdesi (`{"code": "BODY_TOO_LARGE", "max_bytes": 1048576}`) döndürülür.
2. **Dockerfile & Dokümantasyon Temizliği:** `Dockerfile` CMD ve `docs/DEPLOYMENT_CLOUDFLARE.md` içindeki hayalet `--limit-max-request-size` bayrağı temizlendi.

### Ek 7.4 — Kanıt Testleri (`tests/unit/test_body_size_limit.py`)

Kırmızıdan yeşile 8 sözleşme testi kilitlendi:
- `test_uvicorn_rejects_limit_max_request_size_flag` (uvicorn'un bayrağı reddettiği kanıtı)
- `test_dockerfile_cmd_has_no_limit_max_request_size` (Dockerfile CMD denetimi)
- `test_deployment_doc_has_no_invalid_uvicorn_flag` (doküman denetimi)
- `test_middleware_registered_on_app` (middleware kayıt kontratı)
- `test_small_request_passes` (normal istek akışı)
- `test_content_length_oversized_returns_413` (erken 413 testi)
- `test_streaming_body_oversized_returns_413` (parçalı akış kesme testi)
- `test_custom_env_override` (`PINEAL_MAX_BODY_BYTES` dinamik yapılandırma testi)

Sonuç: **8/8 passed**.

---

## Ek 8 — Devir zinciri: kokpit yaması main'e, Ek 1–4 graft hâlâ içerik bekliyor (2026-09-21)

**Bu oturum:** yeni sandbox, push + PR + merge erişimi var (`arena/01a0c461-pineal-clean`). Ek 6.9'un açık kalemleri ve önceki oturumun devir listesi üzerinden çalıştı.

### Ek 8.1 — Devir maddesi 1: Ek 5/6/7 + P0-boot main'de (DOĞRULANDI)

Bu oturum onları main'e taşıyacaktı; main üzerinde **zaten tamam** bulundu ve bu ağaçta işaretleri doğrulandı:
- PR #97 (Ek 5; merge `b2bbe895`), PR #98 (Ek 6; merge `eae21da2`), PR #99 (Ek 7 + P0-boot; merge `cee1686c`) — `gh pr list` çıktısında üçü de MERGED.
- Merge'lerin üstüne iki Bolt performans commit'i işlenmiş: `06124a53` (#95) ve `d570dd82` (#93) → main ucu `d570dd82`, CI yeşil (run `35598568585`, push, success).
- İçerik işaretleri (bu ağaç, grep/ls): `BodySizeLimitMiddleware` → `backend/api.py` (2 yer), `tests/unit/test_body_size_limit.py` mevcut, `_producing_families` → `agent_core/agents/autonomous_verifier.py`, `limit_max_request_size` → `Dockerfile` + `docs/DEPLOYMENT_CLOUDFLARE.md` içinde **0** (Ek 7 temizliği korunmuş).

### Ek 8.2 — Devir maddesi 2: kokpit yaması (UYGULANDI)

Kokpit giriş sahnesi merge edilmemiş `arena/01a0c3e1-pineal-clean` dalında bekliyordu (2 commit, yalnız `frontend/`, PR'sı yoktu). Commit'ler bu ağaca birebir cherry-pick edildi:
- `PinealEye.svelte` (yeni, 440 satır): imleci takip eden canlı göz, göz kırpma, tarama hüzmesi, `eye.jpg` (228 kB) fotoğraf desteği
- `CockpitEntry.svelte` (yeni, 685 satır): izometrik süzülen plakalar, kesik çizgi kolonlar, yörünge halkaları, retina taraması + warp geçişi
- `App.svelte` (+24): açılış perdesi + başlıkta GÖZ düğmesi
- Toplam: 4 dosya, +1149 satır; güvenlik taraması (`fetch`/`eval`/`WebSocket`/`localStorage` yeni bileşenlerde) **temiz**.

**Doğrulama (bu sandbox; node 22.22.3 / npm 10.9.8):** cherry-pick sonrası `frontend/` ağacı `origin/arena/01a0c3e1` ile `git diff` → **boş** (içerik birebir). `npm ci` → OK; `svelte-check` → **0 hata, 0 uyarı**; `vite build` → 138 modül, `dist/assets/*.js` içinde `PINEAL-HERETIC` işareti mevcut. BİLDİRİLDİ: o daldaki tam CI koşuları da yeşildi (run `35598636607`, `35605970542`); birleşik ağacın kendi koşusu bu Ek'i getiren PR'ın CI'ıdır.

### Ek 8.3 — Devir maddesi 3: Ek 1–4 graft (BEKLİYOR — içerik repo dışı)

Defter kuralı 2 gereği slotlar rezerve kalır, bu defter onların yerine içerik uydurmaz. Tarama sonucu: defter dosyası taşıyan **5** uzak dalın (`main`, `01a0c190`, `01a0c336`, `01a0c35d`, `01a0c3e1`) hiçbirinde `## Ek 1` başlığı yok; en güncel sürüm = bu dosya. Denetçi oturumunun 401 satırlık ağacı (Ek 3 @ `:340`) push edilmemişti; içerik yalnız o oturumun sohbet kaydında. Graft, içerik bu depoya gelene kadar **yapılamaz ve yapılmayacaktır**.

### Ek 8.4 — Açık kalemler (Ek 6.9'un güncel hâli)

1. Cloudflare dashboard temizliği — repo dışı, operatör işi (değişmedi).
2. `nltk` izleme — sürüm değişmedi, bekliyor (değişmedi).
3. Altın küme (Ek 5.6) — anahtar gerektirir, hâlâ üretilmedi (değişmedi).
4. Ek 1–4 graft — içerik bekleniyor (Ek 8.3).

### Ek 8.5 — Dürüstlük şerhi (bu Ek için)

- DOĞRULANDI (bu ağaçta; node 22.22.3 / npm 10.9.8): Ek 8.1'in işaretleri (grep/ls), cherry-pick sonrası `frontend/` diff'inin boşluğu, svelte-check 0/0, vite build + `PINEAL-HERETIC` işareti, PR/CI run kimlikleri (`gh` çıktısı).
- BİLDİRİLDİ: `01a0c3e1` daldaki CI run'larının yeşilliği, denetçi sürümünün 401 satır / Ek 3 @ `:340` bilgisi (önceki oturum defter kaydından).
- Bu Ek yeni bir ölçüm sayısı içermez; backend testi koşulmadı çünkü backend ağacı `main`'den değişmedi (`git diff origin/main` yalnız 4 `frontend/` dosyası) — #93/#95 Bolt commit'leri main'de zaten yeşil CI'lıdır (run `35598568585`).
