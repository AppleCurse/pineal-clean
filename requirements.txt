pydantic>=2.10.0
httpx>=0.27.0
open-interpreter>=0.4.0
fastapi>=0.115.0
uvicorn>=0.30.0
playwright>=1.49.0
playwright-stealth>=2.0.0
openai>=2.0.0
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.26.0
aiofiles>=23.2.1
python-dotenv>=1.0.0
# Aşağıdaki ikisi doğrudan kaynak tarafından import edilir (beyan açığı kapatıldı);
# PyYAML: config_loader.yaml — open-interpreter transitif çekse de açık beyan edilir.
PyYAML>=6.0.1
aiohttp>=3.9.0
# OSINT zenginleştirme: profil URL'sinden kararlı kimlik kaydı (MIT, hafif).
# Kütüphane yoksa socid_enricher dürüst available:false döner (çökmez).
socid-extractor>=0.1.0
# OSINT kullanıcı-adı varlık taraması (3300+ site; MIT). Kapı: ENABLE_MAIGRET
# (varsayılan kapalı); kütüphane/DB yoksa dürüst available:false döner.
maigret>=0.6.5
# OSINT e-posta kayıt taraması (deneysel; GPL-3.0 — kod gömülmeden yalnız
# bağımlılık olarak). Kapı: ENABLE_HOLEHE (varsayılan kapalı); istisnaları
# rateLimit maskesi hata sayılır, kapalı ağda 'kayıtlı değil' iddia edilmez.
holehe>=1.61
# Public-web sayfadan LLM-dostu metin (Apache-2.0). Kapı: ENABLE_CRAWL4AI
# (varsayılan kapalı); renderer=http (default) tarayıcı binary'si gerektirmez.
crawl4ai>=0.9.2
# psutil çatışma adjudikasyonu (kanıt: kaynak okuması + çalışma zamanı testi,
# psutil 5.9.8): crawl4ai psutil>=6.1.1 BEYAN eder ama gerçekte yalnız
# Process()/process_iter() kullanır (5.9.8 ile sorunsuz doğrulandı);
# open-interpreter psutil<6.0.0 ister (virtual_memory/disk_usage). Aşırı katı
# metadata çatışması; open-interpreter'ın beyanına saygı için pin'lenir.
psutil>=5.9.6,<6.0.0
