pydantic>=2.10.0,<3.0.0
setuptools>=70.0.0,<81.0.0
httpx>=0.27.0
# [AUDIT 2026-09-11 P0] open-interpreter BURADAN ÇIKARILDI ->
# requirements-interpreter.txt (opsiyonel, ENABLE_INTERPRETER=true).
# Nedeni: open-interpreter 0.4.x wheel metadata'si starlette>=0.37.2,<0.38.0
# dayatıyor; bu pin CVE-2026-48710 "BadHost" düzeltmesini (starlette 1.0.1)
# imkânsız kılıyordu. Kod zaten lazy-import + default-kapalı kapı kullanıyor.
fastapi>=0.141.0
# [AUDIT 2026-09-11 P0] CVE-2026-48710 / GHSA-86qp-5c8j-p5mr ("BadHost"):
# Host header zehirlenmesi request.url.path'i gerçek path'ten saptırıp
# path-tabanlı auth'u atlatıyor; düzeltme starlette 1.0.1. Auth hattı artık
# raw scope path kullansa da (backend/api.py _secure_path) savunma derinliği
# için yamalı alt sınır açıkça sabitlenir.
starlette>=1.0.1,<2.0.0
uvicorn>=0.30.0
playwright>=1.49.0
playwright-stealth>=2.0.0
openai>=2.0.0
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.26.0
aiofiles>=23.2.1
python-dotenv>=1.0.0
# Aşağıdaki ikisi doğrudan kaynak tarafından import edilir (beyan açığı kapatıldı);
# PyYAML: config_loader.yaml — açık beyan edilir.
PyYAML>=6.0.1
aiohttp>=3.9.0
# OSINT zenginleştirme: profil URL'sinden kararlı kimlik kaydı (MIT, hafif).
# Kütüphane yoksa socid_enricher dürüst available:false döner (çökmez).
socid-extractor>=0.1.0
# OSINT kullanıcı-adı varlık taraması (3300+ site; MIT). Kapı: ENABLE_MAIGRET
# (varsayılan kapalı); kütüphane/DB yoksa dürüst available:false döner.
maigret>=0.6.5
# OSINT e-posta kayıt taraması (deneysel; GPL-3.0 — kod gömülmeden yalnız
# bağımlılık olarak). Kapı: ENABLE_HOLEHE (varsayılan kapalı); istisnaları
# rateLimit maskesi hata sayılır, kapalı ağda 'kayıtlı değil' iddia edilmez.
holehe>=1.61
# [KALDIRILDI -> requirements-osint.txt] crawl4ai İKİNCİ ADIM olarak kurulur:
#   pip install -r requirements.txt && pip install -r requirements-osint.txt
# [AUDIT 2026-09-11] open-interpreter core'dan çıkarıldığı için eski
# psutil<6 vs psutil>=6.1.1 ResolutionImpossible çatışması da ortadan kalktı;
# crawl4ai'nin psutil>=6.1.1 beyanı artık tek psutil kaynağıdır.
# Stealth sağlayıcı seçenekleri (STEALTH_PROVIDER; default playwright_stealth).
# invisible-playwright (MIT): patched Firefox launch-level wrapper; pip paketi
# hafiftir, binary İNDİRMEZ — binary opsiyoneldir (INVISIBLE_BROWSER_BINARY).
invisible-playwright>=0.7.4
# GÖREV 2 artığı: sahneleme renk-doygunluğu (HSV-S) piksel ölçümü.
# Kurulum TEK İŞLEMDE kalır (pillow saf wheel); yoksa motor dürüst
# None + not döner (çökmez, uydurmaz).
pillow>=10.0.0
